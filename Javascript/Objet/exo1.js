/*
let character = {
    name : 'Jean-Christophe',
    age : 42,
    items : ['Papier toilette', 'Manche à Balai', 'Poële', 'Rouleau à patisserie', 'Excalibur'],
    giveitem:function(items) {
            return character.items[Math.floor(Math.random()* character.items.length)];
}
}
console.log(character.giveitem());
*/

//for (const key in character) {
  //  console.log(character[key]);
//}

